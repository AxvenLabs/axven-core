name: SEC-113 Apply

on:
  push:
    branches:
      - security-sec113-p2p-status-envelope-clean
    paths:
      - .github/workflows/sec113-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: security-sec113-p2p-status-envelope-clean
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.11'
      - name: Install
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true
          python -m pip install --upgrade pip
          python -m pip install -e .
      - name: Apply and validate SEC-113
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true
          python .github/sec113_apply.py
          python security_sec113_p2p_status_envelope_spec.py
          python release_packaging_test.py
      - name: Commit clean SEC-113 patch
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $PSNativeCommandUseErrorActionPreference = $true
          Remove-Item .github/sec113_apply.py
          Remove-Item .github/workflows/sec113-apply.yml
          git config user.name "AxvenLabs Security Automation"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "SEC-113: Canonicalize P2P status envelope"
          git push origin HEAD:security-sec113-p2p-status-envelope-clean

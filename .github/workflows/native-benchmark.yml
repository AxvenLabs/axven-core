name: Axven Native Benchmark

on:
  push:
    paths:
      - "native/**"
      - "RUST_004.md"
      - "rust_004_native_benchmark.py"
      - "rust_004_native_benchmark_spec.py"
      - ".github/workflows/native-benchmark.yml"
  pull_request:
    paths:
      - "native/**"
      - "RUST_004.md"
      - "rust_004_native_benchmark.py"
      - "rust_004_native_benchmark_spec.py"
      - ".github/workflows/native-benchmark.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  native-benchmark:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    env:
      PYO3_PYTHON: python
      PYTHONUTF8: 1
      PYTHONIOENCODING: utf-8

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13.15"
          check-latest: false

      - name: Install hash-locked Axven runtime dependencies
        run: |
          python -m pip install --no-deps --only-binary=:all: --require-hashes -r requirements-ci-runtime-posix.lock

      - name: Install exact Rust toolchain
        run: |
          rustup toolchain install 1.98.0 --profile minimal
          rustup override set 1.98.0
          python --version
          rustc --version
          cargo --version

      - name: RUST-004 static benchmark boundary
        run: python rust_004_native_benchmark_spec.py

      - name: Build release PyO3 extension
        run: |
          cargo build --release --locked --manifest-path native/axven_native/Cargo.toml --features extension-module
          cp native/axven_native/target/release/libaxven_native.so ./axven_native.so
          python -c "import axven_native; assert axven_native.boundary_version() == 'rust-001'"

      - name: Correctness-first native SMT benchmark
        run: python rust_004_native_benchmark.py
